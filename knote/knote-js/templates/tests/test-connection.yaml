apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "knote-js.fullname" . }}-test-connection"
  labels:
    {{- include "knote-js.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "knote-js.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
